[project]
name = "ship-detection-dota"
version = "1.0.0"
description = "Ship detection on DOTA v1 — practice project"
requires-python = ">=3.10,<3.14"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]